{% deprecated "Você precisa fazer o override do includes/css_assets.html.twig para definir como carregar os assets" %}
<link rel="stylesheet" href="{{ asset('bundles/tabler/font-awesome.css', 'tabler') }}">

{% if tabler_bundle.isRightToLeft() %}
    <link rel="stylesheet" href="{{ asset('bundles/tabler/tabler-rtl.css') }}">
{% else %}
    {# Primeira opção: tabler nativo #}
    <link rel="stylesheet" href="{{ asset('bundles/tabler/tabler-dist.css') }}">

    {# Segunda opção: tabler completo, para uso genérico #}
    <link rel="stylesheet" href="{{ asset('bundles/tabler/tabler-full.css') }}">

    {# Terceira opção: tabler especializado em CRUD, listagens #}
    <link rel="stylesheet" href="{{ asset('bundles/tabler/crud-list.css') }}">

    {# Quarta opção: tabler especializado em CRUD, formulários #}
    <link rel="stylesheet" href="{{ asset('bundles/tabler/crud-form.css') }}">

    {# Quinta opção: tabler incorporado. Provavelmente a opção mais interessante para seu projeto #}
    {{ encore_entry_link_tags(entrypoint|default('tabler-full')) }}

{% endif %}